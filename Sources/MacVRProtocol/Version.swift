import Foundation

/// Human-readable release version for the public macVR package.
public let macVRReleaseVersion = "0.1.0"

/// Wire-level protocol version for control and frame transport messages.
public let macVRProtocolVersion = 1
